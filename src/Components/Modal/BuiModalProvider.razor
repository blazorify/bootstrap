@namespace Blazorify.Bootstrap
@inherits BuiComponentBase

@if (modalService != null && modalService.ModalFragments.Count > 0) {
	foreach (var modalFragment in modalService.ModalFragments) {
		@modalFragment
	}

	<div class="modal-backdrop fade show"></div>
}
